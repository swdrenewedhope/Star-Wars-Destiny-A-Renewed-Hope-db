security:
    encoders:
        FOS\UserBundle\Model\UserInterface: sha512

    role_hierarchy:
        ROLE_ADMIN:       ROLE_USER
        ROLE_SUPER_ADMIN: ROLE_ADMIN

    providers:
        fos_userbundle:
            id: fos_user.user_provider.username_email

    firewalls:
        oauth_token:
            pattern:    ^/oauth/v2/token
            security:   false

        oauth_authorize:
            pattern:    ^/oauth/v2/auth
            form_login:
                provider: fos_userbundle
                check_path: oauth_server_auth_login_check
                login_path: oauth_server_auth_login
            anonymous: true

        api:
            pattern:    ^/api
            fos_oauth:  true
            stateless:  true
            anonymous:  true

        dev:
            pattern:    ^/(_(profiler|wdt|error)|css|images|js)/
            security: false

        default:
            pattern:    ^/
            form_login:
                provider: fos_userbundle
                csrf_token_generator: security.csrf.token_manager
                always_use_default_target_path: false
                default_target_path: index
            logout:       true
            anonymous:    true
            remember_me:
                secure:   "%remember_me_secure%"
                secret:   "%secret%"
                lifetime: 31536000
                path:     /
                domain:   ~
                always_remember_me: true
            
    access_control:
        - { path: ^/login$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/register, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/resetting, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/remind, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/admin/, role: ROLE_ADMIN }
        - { path: ^/js/, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/$, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/deck/view/, role: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/deck/, role: ROLE_USER }
        - { path: ^/decks, role: ROLE_USER }
        - { path: ^/user/, role: ROLE_USER }
        - { path: ^/collection/, role: ROLE_USER }
        - { path: ^/api/decks, role: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/deck/save, role: IS_AUTHENTICATED_FULLY }
        - { path: ^/api/deck/publish, role: IS_AUTHENTICATED_FULLY }
        - { path: ^/api, role: IS_AUTHENTICATED_ANONYMOUSLY }
